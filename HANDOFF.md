# FR3-WMS 로봇 용접 제어 시스템 — 인수인계 문서

새 Claude 세션이 이 프로젝트를 이해하기 위한 문서. 2026-09-09 기준.

## 1. 프로젝트 개요

FR3(Fairino) 로봇 기반 용접 제어 시스템. 상용화(여러 고객사 배포) 계획 중.

**구성:**
- `robot-core`: C++, Fairino SDK 사용, 실행파일 `robot_core.exe`. httplib 기반 내장 서버로 `robot-front` 정적 빌드를 서빙(localhost:8080).
- `robot-front`: React/TypeScript.
- `robot-back`: Python(→exe), 갭 파라미터 백엔드.
- DB: MariaDB. 필드 PC는 12.3, 개발PC는 11.4 (버전 다름, 별도 인스턴스).

**PC 명칭:**
- **노트북** = 필드 PC. 실제 로봇이 물리적으로 연결된 현장 PC. 자동 업데이트로 배포받음.
- **개발PC** = D113964. 연결된 폴더 `C:\Users\D113964\Desktop\git\robot`. 빌드/릴리즈 작업하는 PC. 자체 로컬 개발용 DB 있음.

## 2. 빌드/릴리즈 절차

저장소 루트에 `release.ps1` 스크립트로 자동화되어 있음.

```powershell
.\release.ps1 -Version 1.1.97 -Notes "변경 내용"
```

이 한 줄이 하는 일: robot-core 빌드(`build_unity.bat`) → robot-front 빌드(`npm run build`) → `iscc installer.iss` → SHA256 해시 생성 및 `.sha256` 저장 → `gh release create`까지 전부 실행. 중간 실패 시 그 단계에서 중단됨.

**중요:** 이 스크립트는 버전 문자열을 자동으로 올려주지 않음. 실행 전에 아래 3곳을 반드시 먼저 수동으로 동일 버전으로 맞춰둬야 함.

1. `robot-core/src/robot_core_all.cpp` — `#define APP_VERSION_STRING "x.x.x"`
2. `robot-front/src/lib/index.ts` — `export const APP_VERSION = 'x.x.x';`
3. `installer.iss` — `#define MyAppVersion "x.x.x"`

같은 버전 번호로 자산(에셋)만 바꾸면 자동 업데이터가 새 버전으로 인식 못 함. 반드시 숫자를 올려야 함.

`release.ps1` 실행은 사용자가 자신의 PowerShell에서 직접 함 (Claude는 사용자 PC에 직접 셸 접근 권한이 없어 스크립트 유지보수만 담당, 사용자가 실행).

DB 마이그레이션이 포함된 변경이면, 릴리즈 전에 노트북 MariaDB에도 먼저 반영해야 함.

**PowerShell 관련 주의사항:**
- `&&` 대신 줄바꿈이나 `;` 사용 (Windows PowerShell 5.1 기준). `&&`로 이어 쓰면 앞 명령이 실패해도 다음 줄이 그냥 진행되는 사고가 실제로 있었음(v1.1.70/71에서 빌드 실패한 stale 빌드가 배포됨).
- mysqldump 백업 시 `>` 리다이렉션은 한글 포함 덤프를 인코딩 깨뜨림(UTF-16LE로 저장됨) → `--result-file=` 옵션 사용.
- mysql -e로 이중 인용부호 포함된 UPDATE/JSON 문자열 실행 시 PowerShell 인용부호 처리가 불안정 → here-string(`@'...'@`)으로 `.sql` 파일 만들어서 `source` 하는 방식이 안전.
- mysql client의 `source` 명령에 경로 넘길 때 백슬래시(`C:\Users\...`)는 `\U` 같은 걸 클라이언트 메타명령으로 오인식함 → 슬래시(`C:/Users/...`)로 바꿔서 넘길 것.

## 3. 현재 상태 (2026-09-09 기준, 이번 세션 작업 결과)

### 3-1. v1.1.34로 전체 롤백

수개월간 v1.1.34 이후로 누적된 기능 추가(8점 티칭 스키마, 파트 전환 안전 후퇴 패턴, WELD_BATCH_SPEED_SCALE 튜닝 등) 이후 용접 실패와 터치센싱 오류가 계속 발생해서, 사용자가 v1.1.34로 전체 롤백을 결정함.

**주의: git 태그 검색 시 함정.** `git log --oneline --all | Select-String "1.1.34"`는 커밋 메시지에 그 문자열이 들어간 커밋을 찾는 것이지, 실제 v1.1.34 태그가 붙은 커밋을 찾는 게 아님. 실수로 다른 버전(v1.1.64)의 커밋을 v1.1.34로 착각해 리셋한 적 있음. **반드시 `git tag -l | Sort-Object`로 실제 태그 목록을 확인하고 `git reset --hard v1.1.34`처럼 태그명으로 리셋할 것.**

v1.1.34 태그 커밋(`bfa28fb`)에 내부 `APP_VERSION_STRING`은 실제로 "1.1.30"으로 박혀있었음(과거 태깅 시점의 오래된 불일치, 지금은 무의미).

**안전망:** 리셋 직전에 `backup-before-1.1.34-rollback` 브랜치를 만들어 그 이전 전체 히스토리(release.ps1 포함)를 보존해둠. `release.ps1`은 v1.1.34 시점엔 없던 파일이라 `git checkout backup-before-1.1.34-rollback -- release.ps1`로 다시 가져옴.

### 3-2. 자동 업데이트 트릭으로 배포

노트북에 수동 설치 파일을 옮기는 대신, v1.1.34의 실제 코드는 그대로 두고 버전 문자열 3곳만 노트북에 이미 설치된 버전(1.1.95)보다 높은 숫자로 올려서, 앱 자체의 자동 업데이트 메커니즘이 "더 새 버전"으로 인식하고 설치하게 하는 방식을 사용함. 처음엔 "1.1.96"으로 릴리즈해서 성공적으로 설치 확인함.

**핵심: 버전 문자열만 다르고 코드/UI/기능은 실제 v1.1.34와 100% 동일함.** "96버전 UI가 왜 34버전이랑 똑같냐"는 질문에 대한 답 — 애초에 96은 34의 코드 그대로에 숫자만 바꾼 것이라 당연히 동일함. v1.1.34에 없던 기능은 UI든 백엔드든 아예 존재하지 않음("깡통" 아니라 "무존재").

버전 문자열 교체 시 한글 주석이 섞인 파일을 다루려면 PowerShell에서 `[System.Text.Encoding]::GetEncoding("ISO-8859-1")`로 바이트 단위 1:1 읽기/쓰기를 하면 한글을 안 건드리고 ASCII 버전 숫자만 안전하게 치환 가능(각주: `[System.IO.File]::ReadAllBytes()` 같은 .NET API를 PowerShell에서 쓸 땐 상대경로가 아니라 항상 절대경로로 넘길 것 — CWD 불일치 버그 있음).

### 3-3. DB 복원 관련 사고와 수정

노트북 DB를 마이그레이션 이전 상태로 되돌리는 과정에서, 이전 세션에 만들어뒀던 "인코딩 수정본" 백업 파일(`backup_robot_welding_field_fixed.sql`)이 실제로는 버그가 있어서 **p9~p12 데이터가 전부 유실된 파일**이었음이 밝혀짐(사용자가 "9월 4일엔 분명 12포인트 있었다"고 강하게 반박해서 재검증하게 됨 — 결과적으로 사용자가 맞았음).

원본 raw 백업 파일(`backup_robot_welding_field_20260908_131514.sql`)은 사실 UTF-16LE로 저장돼 있었고(PowerShell `>` 리다이렉션의 기본 인코딩), 이걸 `Get-Content -Raw -Encoding Unicode | Set-Content -Encoding UTF8`로 다시 제대로 변환해서 재복원함. 복원 후 job 435 기준 12포인트 전부 정상 확인.

admin 계정 이름이 깨져 보이는 문제("愿由ъ옄")는 원본 raw 백업에도 동일하게 있던 걸 확인 — 오늘 작업과 무관한 오래된 기존 데이터 손상. 급하지 않음.

### 3-4. getState/checkConnection 오탐 "연결 불가" 수정 (v1.1.97, 이번 세션에서 재적용)

v1.1.34로 롤백하면서, v1.1.61에서 이미 고쳤던 버그가 다시 되돌아옴: `getState()`와 `checkConnection()`이 `std::lock_guard<std::mutex>`로 블로킹 락을 잡는데, `moveL()` 같은 긴 이동 명령도 이동 완료까지 같은 `m_mutex`를 계속 점유함. 그래서 실제로는 로봇이 정상 연결·이동 중인데도, 동시에 들어오는 상태조회(`/robot_sdk/realtime` 폴링 등)가 락을 못 잡아 블로킹되면서 "연결 실패"/"TCP 데이터 없음"으로 오탐 표시됨.

**v1.1.61 원본 수정 방식**(당시엔 더 복잡한 `m_stopRobot` 분기 구조 위에 적용됐었음)을 v1.1.34의 더 단순한 구조에 맞게 재적용함:

- `getState()`: `std::lock_guard` → `std::unique_lock<std::mutex> lock(m_mutex, std::try_to_lock)`. 락을 못 잡으면 즉시 포기하고 캐시(`m_cachedState`, 별도 `m_stateCacheMutex`로 보호)에 저장된 마지막 상태를 반환.
- `checkConnection()`: 동일하게 try_to_lock. 락을 못 잡으면(=이동 중이라는 뜻) `true`(연결됨)를 즉시 반환.

이미 `m_cachedState`/`m_stateCacheMutex`는 v1.1.34 코드에 원래 존재했음(`getCachedState()`가 쓰고 있었음)이라 새 멤버 변수 추가는 필요 없었음.

**적용 및 릴리즈 완료:** `robot_core_all.cpp` 두 함수 수정, 버전 3곳 모두 **1.1.97**로 올려서 `.\release.ps1 -Version 1.1.97 -Notes "..."` 릴리즈까지 완료됨.

### 3-5. 빌드 사고: `robot_core_all.cpp`가 v1.1.34가 아니었던 문제 (해결됨, 중요)

위 3-4 수정을 처음 커밋하고 빌드했을 때 100개 넘는 컴파일 에러 발생(`setHangCallback`, `startWatchdog`, `newSplineStart/Point/End`, `m_stopRobot`, `pointsOffsetEnable`, `setToolCoord` 등 다수 "RobotService의 멤버가 아님"). 원인 조사 결과:

- `git log --oneline -1` / `git status`로 확인해보니 HEAD는 정확히 v1.1.34(`bfa28fb`)였고, `git diff --stat HEAD -- robot-core/src/robot_core_all.cpp`가 **1049줄 추가/255줄 삭제**를 보여줬음. 즉 `robot_core_all.cpp`가 **v1.1.34로 리셋된 뒤 커밋 안 된 채로 그보다 훨씬 나중 버전의 코드가 다시 섞여 들어간 상태**였음(어느 시점에 어떻게 섞였는지는 특정 못 함 — 이전 세션의 어느 작업 중 실수로 최신 버전 파일이 덮어써진 것으로 추정).
- `robot_core_all.h`(헤더)는 진짜 v1.1.34 그대로였어서 그 안에 없는 멤버를 `.cpp`가 참조하며 에러 발생.
- `build_unity.bat`는 단순 CMake+Ninja 빌드일 뿐, 파일을 재생성하는 스크립트가 아님(확인함, "unity build"는 그냥 단일 거대 .cpp 컴파일 방식을 뜻함) — 그래서 빌드가 원인이 아니었음.
- **해결:** `git checkout -- robot-core/src/robot_core_all.cpp`로 파일을 깨끗한 v1.1.34로 되돌린 뒤, getState/checkConnection 수정과 버전 문자열만 다시 적용. 재적용 후 `git diff --stat HEAD -- robot-core/src/robot_core_all.cpp`가 **22줄(18줄 추가/4줄 삭제)**만 나오는 것으로 정상 확인 후 빌드/릴리즈 성공.

**교훈:** git 히스토리상 문제없어 보여도(HEAD가 맞는 태그) **작업트리(uncommitted 변경사항)가 실제로 그 커밋과 일치하는지 `git status`/`git diff --stat HEAD`로 항상 별도 확인할 것.** `.cpp` 파일을 device에서 가져와 Read/Edit하기 전에, 특정 함수 몇 개만 grep해서 "맞겠지" 판단하지 말고 전체 diff 크기를 먼저 확인하는 습관이 필요함.

### 3-6. 빌드 사고 2: `device_commit_files`로 썼다고 나온 파일이 실제로는 반영 안 된 사례 (해결됨, 중요)

3-5를 고치고 release.ps1을 처음 돌렸을 때, robot-core 컴파일은 성공했는데 Inno Setup 단계에서 `RobotWeldingControl_Setup_1.1.97.exe`를 못 찾고 대신 `RobotWeldingControl_Setup_1.1.95.exe`가 만들어짐 — `installer.iss`의 `MyAppVersion`이 "1.1.97"로 수정 후 device에 커밋(`device_commit_files`가 "written" 성공 응답을 줬음)했는데도, 실제 device 파일을 다시 가져와 보니 "1.1.95"로 되어있었음(`robot-front/src/lib/index.ts`도 동일 증상). 정확한 원인은 특정 못 함(도구가 "성공"이라고 응답했는데 실제로 반영 안 된 케이스).

**해결:** 두 파일을 다시 "1.1.97"로 수정 → 커밋 → **커밋 직후 바로 다시 device에서 가져와서(re-stage) 실제 내용이 반영됐는지 확인**한 뒤에야 사용자에게 재빌드 요청. 재확인 후엔 정상적으로 v1.1.97 릴리즈 성공함.

**교훈:** `device_commit_files`가 "written" 성공을 반환해도 그대로 믿지 말 것. **버전 문자열처럼 빌드 결과에 직접 영향 주는 값은 커밋 직후 반드시 다시 `device_stage_files`로 재확인**할 것 — 특히 이번 세션에서 파일이 예상과 다른 상태였던 사례(3-5, 3-6)가 두 번이나 나왔으므로, 앞으로 이 프로젝트에서 device 파일을 고칠 때는 "수정 → 커밋 → 재확인" 3단계를 습관화할 것.

### 3-7. 파트 전환(p6→p9) 특이점 문제 — 용접 테스트에서 재현, 임시 수정 (v1.1.98)

용접 테스트 중 파트 전환(6번 포인트→9번 포인트) 구간에서 매번 "파트 전환 후퇴 이동 실패"로 중단되고 비상종료까지 이어지는 문제 발생. robot-core 서버 로그 확인 결과 원인은 SDK 에러 `code=38`(`ERR_STRANGE_POSE`, 특이점 자세 — `fairino-cpp-sdk-main/windows/libfairino/include/robot_error.h` 참고).

**원인:** p6, p9 포인트 자체는 문제 없음 — 사용자가 직접 "여기로 이동"으로 6→9를 테스트해보면 에러 없이 잘 감. 문제는 `weldingExecution.ts`의 파트 전환 로직이 두 포인트 사이에 실제 티칭 안 된 중간 "후퇴" 좌표를 계산해서 끼워넣는 것 — `prevPoint.tcp`에 `CROSS_CLEARANCE_X`(150)/`CROSS_LIFT_Z`(100) 오프셋을 더한 좌표로 `moveToCartesianPosition`(MoveL, 직선보간)을 시도하는데, 이 계산된 좌표가 그 툴 자세 기준으로 특이점에 걸림. v1.1.34 원본은 이 이동이 실패하면 바로 `throw`해서 용접 전체가 중단되는 구조였음.

**비교 확인:** 롤백 전 마지막 상태(백업 브랜치 `backup-before-1.1.34-rollback`, 커밋 `44ca759`, 버전 1.1.94)에는 이미 이 문제의 수정이 들어가 있었음 — 후퇴 이동을 MoveL 대신 `prevPoint.joints` 기준 IK+MoveJ로 우회하고, 그래도 실패하면 throw 대신 경고 로그만 남기고 계속 진행하도록 바뀌어 있었음(2026-09-04자 커밋 주석에 동일한 code=38 문제가 명시돼 있었음). 다만 이 버전은 다른 누적 문제(터치센싱 오류 등) 때문에 롤백된 버전이라 통째로 가져오지 않고, 이 로직만 별도로 좁게 재구현함.

**적용한 임시 수정:** `weldingExecution.ts`의 파트 전환 블록에서 `prevPoint.id === 'p6' && point.id === 'p9'`인 경우에만 특별 분기 추가 — 후퇴(①)/접근(②)/정위치(③) 3단계를 전부 생략하고 `point.joints`로 `moveToJointWithStopCheck`(MoveJ) 한 번만 실행. 다른 모든 파트 전환(같은 side든 다른 크로스 사이드 조합이든)은 기존 v1.1.34 로직 그대로 유지 — 이 특정 구간에만 좁게 적용된 예외 처리.

**되돌리는 법:** `weldingExecution.ts`에서 `if (prevPoint?.id === 'p6' && point?.id === 'p9' ...) { ... }` 블록 전체(`const pointSide = ...` 선언 바로 위, 약 20여 줄)를 삭제하면 원래 v1.1.34 동작으로 복귀.

**검증 상태:** 사무실에서 "용접 테스트"(아크 OFF, `isWeldingTest` 모드)로 재현 및 수정 확인함 — 수정 후 에러 없이 통과. **실제 용접(아크 ON, 필드 배치)에서는 아직 검증 안 됨.** 릴리즈용 버전 문자열 변경 스크립트(1.1.98)는 전달했으나 실제 `release.ps1` 실행 여부는 다음 세션에서 확인 필요.

**주의:** 이 방식(taught point 간 직접 MoveJ)은 관절보간이라 3D 충돌 회피를 보장하지 않음(§5-5 참고). p6→p9 구간은 사용자가 직접 이동으로 실측 확인했지만, 다른 크로스 사이드 전환에 이 방식을 확장 적용하려면 각각 개별적으로 실물 확인이 필요함. 또한 실제 용접(아크 ON) 시에는 로봇 속도·타이밍이 테스트 모드와 달라서(§3-4/§4 참고, WeldBatch 속도 계산식이 별개 이슈로 남아있음) 이 구간에서 다른 양상이 나올 가능성을 배제할 수 없음.

## 4. v1.1.34 이후 버전별 히스토리 요약 (참고용, 지금은 코드에 없음)

이번 세션에서 롤백했기 때문에 아래 항목들은 **현재 코드에 존재하지 않음**. 나중에 "어떤 기능을 다시 선별 적용할지" 판단할 때 참고용.

- **v1.1.18~61**: 보안 리뷰 수정, R창 응답없음 수정(connect를 백그라운드 스레드로), WeldBatch code=14/74 에러 근본원인 수정(moveL 시 joint/tcp 불일치), **노란 연결불가 오탐 배너 수정(=오늘 재적용한 try_to_lock)**, 종료 워치독.
- **v1.1.62~65**: 터치센싱 소요시간 표시, WELD_BATCH_SPEED_SCALE 튜닝.
- **v1.1.66~69**: 배치이동 타임아웃 연장, 파트 전환 특이점 회피(IK+MoveJ), 파트 순서/포인트 방향 DB 수정.
- **v1.1.70~84**: 후퇴/상승(executeSafeLift, executeRetract) 로직 여러 차례 시도·원복 반복 — 홈↔우측 이동 시 앞벽 충돌 문제는 **결국 소프트웨어만으로 미해결**(MoveJ 관절보간이 3D 충돌 회피를 보장 못함). 남은 방법은 안전 경유점 티칭 또는 조그 UI 신규 구현뿐(둘 다 보류 상태였음).
- **v1.1.75**: 파트 첫 이동 구간 전류 과다 현상에 FIRST_MOVE_SPEED_BOOST(+20%) 보정.
- **v1.1.85**: 파트 전환 특이점 문제, CROSS_CLEARANCE_X/CROSS_LIFT_Z 오프셋 조정으로 해결.
- **v1.1.86**: 용접 시작점 접근에도 IK+MoveJ 안전 패턴 적용(우측 시작점 특이점 대응).
- **v1.1.87~89**: 매 포인트 정지 문제를 Fairino SDK Spline 방식으로 해결 시도했으나 **실기 실패**(SDK/펌웨어가 스플라인 이동을 제대로 지원 안 하는 것으로 추정) → moveL 순차 호출 방식으로 원복. 이 문제 자체는 미해결로 남음.
- **v1.1.90**: 티칭 스키마 12점→8점 축소(중간점 제거, 코너점만). DB 마이그레이션 포함.

## 5. 미해결/보류 항목 (v1.1.34 롤백 이전 기준, 참고용)

1. ~~용접 중 와이어 조정(Wire In/Out) 무반응~~ — v1.1.126에서 forwardWireFeed/reverseWireFeed의 mutex를 emergencyStop과 동일하게 제거해서 시도함. 아직 실전 필드 테스트로 "용접 도중 실제로 반응하는지" 검증 안 됨. §8 참고.
2. 매 포인트 정지 문제 — moveL이 포인트마다 완료까지 블로킹돼 다음 명령 도착 전 궤적 큐가 비어 정지. Spline 시도 실패, 원인은 파악됐으나 미해결.
3. 용접 중 "현재 포인트" 표시가 실제와 안 맞음 — batchMoveL 단일 HTTP 요청 특성상 중간 실시간 갱신 불가.
4. FAIRINO 웹설정 페이지 "로봇 활성화" 버튼 비활성 — 원인 미상, 급하지 않음.
5. 홈↔우측 이동 시 앞벽 충돌 — 소프트웨어만으론 미해결(§4 참고).

## 6. 이번 세션에서 배운 원칙

- 사용자가 "원래 이렇지 않았다"처럼 결과에 강하게 반박하면, 먼저 내 진단을 의심하고 데이터로 재검증할 것 — 이번에 p9~p12 유실 백업 버그를 이렇게 찾음.
- "연결 문제처럼 보이는 로그"라도 사용자가 "로봇은 실제로 붙어있다"고 하면 그 전제를 믿고 원인을 다시 찾을 것 — 로그만으론 실제 연결 여부를 못 판단하는 케이스가 실재함(mutex 블로킹).
- 버전 문자열만 바꿔 자동업데이트로 배포하는 방식은 코드/기능이 100% 동일하다는 점을 명확히 인지할 것.

## 7. 다음 세션에서 확인할 것

1. **터치센싱 접근 속도 체감 저하 (미확인, 보류)** — 사용자가 "v1.1.34에서 터치센싱 하러 가는 속도가 느려진 것 같다"고 함. git 확인 결과 `touchSensingPointSpeed` 코드 기본값(50)과 사용 방식은 v1.1.34-44ca759(1.1.94) 사이에 변화 없음 — 이 값은 DB(`robot_settings.touch_sensing_point_speed`)에서 읽어오는 구조라 코드 롤백과 무관하게 DB에 저장된 실제 값이 낮을 가능성이 있음. 참고로 44ca759 쪽(v1.1.93)엔 8포인트 스키마로 포인트 간 거리가 멀어지면서 벽/지그 충돌 방지용 후퇴 스텝이 추가로 들어가 있어 오히려 그쪽이 동작이 더 많음(=더 느려짐) — 즉 지금 v1.1.34가 코드상 더 적은 스텝인데도 느리게 느껴진다면 DB 설정값 쪽을 의심해봐야 함. 사용자가 실제 티치센싱 테스트를 해보고 다시 판단하기로 함 — 결과 듣고 DB `robot_settings` 값 조회 이어서 진행할 것.

## 8. 2026-09-14 세션 요약 (v1.1.120 → v1.1.128)

이번 세션에서 순서대로 처리한 항목들. 최신 배포 버전은 **v1.1.128**.

1. **v1.1.121** — installer.iss에 `SetupLogging=yes` 추가(설치 파일 교체 안 되는 문제 진단용). 이후 문제가 재현 안 돼 로그 분석은 안 함. 필요 없으면 다음에 제거해도 됨.
2. **v1.1.122** — 터치센싱 완료 팝업에 "소요 시간" 안 뜨던 버그. `touchSensing.ts`에서 `totalTimer.end()`가 반환하는 소요시간을 메시지 텍스트에 아예 안 넣고 있었음. 반영 완료.
3. **v1.1.123** — 실용접 안전 이슈: HOME에서 바로 p9/p10으로 시작할 때(중간 포인트 생략) MoveJ 곡선 경로가 U셀 구조물에 와이어를 부딪히게 함. p9/p10을 시작점으로 삼을 때만 MoveJ 대신 MoveL(직선)로 접근하도록 `weldingExecution.ts` 수정. 필드 테스트로 해결 확인됨.
4. **v1.1.124** — "마지막 작업 자동 로드" 기능이 이미 코드에 있었는데 빈 catch 블록이 에러를 완전히 삼켜서 원인 파악 불가였음. 에러 로깅만 추가(동작 자체는 안 바꿈) — 다음에 재현되면 로그로 원인 확인 가능.
5. **용접 속도 저하 (수직 30cpm→225초, 수평 50cpm→200초 나와야 하는데 DryRun 기준 3~4배 느리게 나옴)** — 3단계로 원인 파악, 순서대로 수정:
   - **v1.1.125**: `robot_core_all.cpp`의 WeldBatch `per_point` 분기 `OVERSPEED_ADAPTIVE` 값이 3(적응형 감속)으로 되어 있었음. v96/v112 백업 코드는 0(v1.1.54에서 의도적으로 확정한 값, 주석에 이유 남아있음). 0으로 복원. 개선은 됐으나(4.3배→3.6배) 부족.
   - **v1.1.127**: 같은 `per_point` 분기에서 포인트에 joints가 있으면 `moveLWithJoints`(joint_pos+desc_pos 둘 다 전달)를 쓰도록 되어 있었음. v96/v112는 v1.1.56에서 일부러 joints를 버리고 tcp만으로 `moveL` 호출하도록 고쳐놨던 부분(SDK가 joint_pos/desc_pos 불일치 시 code=74 ERR_LINE_POINT를 내던 문제 회피) — 이게 나중 리팩토링으로 되돌아가 있었음. v96과 동일하게 tcp only로 재복원. **DryRun 재테스트로 실제 개선됐는지 아직 확인 안 됨 — 다음 세션에서 최우선 확인.**
   - 참고: 실제 용접(아크 ON, `per_point=false`, 단일 MoveL 분기)의 코드는 v96/v112와 이 시점까지 100% 동일함(moveLWithJoints 사용도 원래부터 있었음) — 그래서 이 버그는 DryRun 쪽에만 해당하고, 실제 용접이 느린 건 별개로 검증 필요(비상정지 없이 깨끗한 실측 필요, 아직 못 함).
6. **v1.1.126 — 용접 중 와이어 조정 기능(신규, 테스트 단계)** — 요청 배경: 용접 중 와이어(팁-모재 간 돌출 길이)가 빨리 닳아서 용접 품질 저하·전류 과다. 기존에도 Wire In/Out 버튼은 있었지만 `forwardWireFeed`/`reverseWireFeed`가 `moveL`과 같은 mutex를 써서, 용접 이동(블로킹 MoveL)이 도는 동안은 눌러도 반응 안 함(§5-1 참고). `emergencyStop`/`stopMotion`과 동일하게 mutex를 뺀 버전으로 만들어봄 — **SDK/컨트롤러가 모션 도중 동시 명령을 실제로 받아주는지 미검증, 필드 테스트 필요. 안 되면 롤백.**
7. **v1.1.128** — 위 기능과 별개로 발견한 문제: `/welding/arc/off` 핸들러가 전류/전압 AO(아날로그 출력)를 아크온 시 설정만 하고 리셋을 안 해서, 용접기 자체 송급속도 연동 로직 때문에 "용접 직후" 수동 와이어 조정이 비정상적으로 빠르고 길게 나가는 버그가 이전 세션에 이미 발견/수정됨(자세한 내용은 memory 참고, 이 문서엔 없음). 오늘 이 버그의 연장선으로 "용접 도중"에도 같은 이유(전류 AO가 실제로 높은 상태)로 와이어 버튼이 빠르게 나갈 수 있다고 판단 — 안전 목적으로 `useWireControl.ts`의 1회 피딩량을 1mm→0.2mm로 임시 축소. 실측 후 재조정 필요.

**다음 세션 최우선 확인 사항 (순서대로):**
1. v1.1.127의 DryRun 속도 재테스트 — "총 이동거리/예상/실제/차이" 팝업으로 225초/200초 목표에 얼마나 가까워졌는지 확인.
2. 실제 용접(아크 ON)을 비상정지 없이 깨끗하게 1회 돌려서 실측 시간 확보 — 지금까지 실측은 전부 용접 실패로 인한 비상정지가 섞여 있어 신뢰 불가.
3. v1.1.126 mutex 제거가 실제로 용접 도중 Wire In/Out 버튼을 반응하게 하는지 필드 테스트. 안 되면 mutex 되돌리기(git revert).
4. v1.1.128의 0.2mm 축소가 용접 중 기준으로 적당한지 실측 후 조정.
