v1.1.168

- 포인트 GAP 입력 시 자동 불러오기 확장
  gap 0(기본값)도 불러온다. 이전에는 0보다 클 때만 동작했다.
  전류/전압/속도에 더해 위빙(종류, 주파수, 진폭, 좌우 체류)까지 적용한다.
  수평 우측(P10~P12)은 좌우 체류를 뒤집어 적용한다.
  좌/우 진폭은 표에 없어 포인트의 현재 값을 유지한다.
- 연속 입력 시 늦게 도착한 이전 조회 결과가 덮어쓰지 않도록 수정
- 규칙을 공용 함수(gapParamApply.ts)로 분리 (이후 펜던트 화면에서 재사용)
- 설치 폴더에 DB 마이그레이션 스크립트 포함 (database\migrations)

[DB 마이그레이션 011 - 설치 후 노트북에서 1회 실행]
갭 파라미터 표(welding_params 등 10개)가 노트북 DB에 없었다.
없는 표만 만들고 gap 0 = 2026-09-21 현장 기본값 8행(자세 2 x 판두께 4)을 넣는다.
다시 실행해도 중복되지 않는다.
