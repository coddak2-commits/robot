v1.1.186

- 수평 갭 칩이 더 내려가지 않던 원인 수정
  칩은 캔버스 밖으로 못 나가게 제한되는데(clamp), U셀이 캔버스를 꽉 채워
  아래 여백이 없어서 이미 한계에 닿아 있었다. 작업 영역(BOUNDS)을 +-400에서
  +-440으로 넓혀 U셀 아래 여백을 확보하고, 제한 여백도 26 -> 14로 줄였다.
  수평 갭 칩 오프셋은 175 -> 120(실제로 더 내려간다).
- 갭 칩 +/- 글자 크기 8 -> 14 (버튼 10x10 크기는 그대로)
